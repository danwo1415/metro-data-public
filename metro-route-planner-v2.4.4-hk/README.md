# 六城地鐵路線規劃器 v2.4.4-HK

## 本次修正
- App 每次啟動直接下載並套用公開的 `serviceHours`，不再依賴刷新後重新載入頁面。
- 下載成功後立即在記憶體中更新首末班並重新計算畫面。
- 本機儲存僅作快取；即使 localStorage 寫入失敗，當次仍可正常顯示。
- 狀態列顯示實際載入的車站數與方向紀錄數，例如 `95站／318條`。
- 刷新不再呼叫 `location.reload()`，避免 WebView 重新載入失效。
- 增加方向終點容錯查找，處理分支線和終點鍵不完全一致的情況。
